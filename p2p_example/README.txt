compilation of the sources requires:

boost  (http://www.boost.org/)
json-c (http://oss.metaparadigm.com/json-c/)

once those are on the path, run make and the executables peer and tracker
will be compiled.

Running either of the executables with no arguments will print out their usage.

